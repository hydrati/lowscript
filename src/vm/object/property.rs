use super::{Trace, Finalize};
use std::default::Default;
use core::fmt::Debug;
use rustc_hash::FxHashMap;

#[derive(Debug, Default, Trace, Finalize)]

pub struct PropertyDescriptor {

}

#[derive(Debug, Default, Trace, Finalize)]
pub struct PropertyMap {

}